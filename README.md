# Python-Piscine

## Tests

Run the subject-level smoke tests for `starting_0/ex00` through `starting_0/ex08`:

```sh
python3 -m unittest discover -s starting_0/tests
```

Or from inside `starting_0/`:

```sh
python3 -m unittest discover -s tests
```

See [starting_0 test documentation](docs/starting_0_tests.md) for what each test covers.
